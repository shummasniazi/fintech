# ISO 8583 sale message (moved)

This topic is fully documented here:

**[PACKET_BUILDING.md](PACKET_BUILDING.md)**

That page covers the complete packet pipeline, `ISOFieldModel`, `InterfaceInstances`, `ISO8583Constants`, `ISO8583DomainConfig`, `ISO8583DomainModel`, `ISO8583Message`, and every sale data element included in the request path.
